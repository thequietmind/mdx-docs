import Alert from "@mui/material/Alert";

import Banner from "../components/Banner.jsx";
import Lead from "../components/Lead.jsx";

# Examples

<Lead>
  Real documentation patterns you can learn from and adapt — each built with MDX,
  React, and Material UI.
</Lead>

<Banner severity="info" title="Examples, not features">
  Nothing in this section ships with the <code>@quietmind/mdx-docs</code> package.
  These are patterns and components you can copy into your own documentation site.
</Banner>

## Patterns

### [Design System &amp; Theme](/design-system)

Document your color tokens and type scale directly from your live theme, so the
reference always matches the real thing — a great home page for a design system.

### [Documentation Components](/components)

Reusable building blocks — lead text, banners, hero sections, and feature grids —
each shown with a live preview and copy-paste source.

## Embedding React in MDX

Because every page is MDX, you can drop any React or Material UI component
straight into your content:

<Alert severity="success">
  This Alert is a live React component rendered inside Markdown.
</Alert>

```mdx
import Alert from "@mui/material/Alert";

<Alert severity="success">
  This Alert is a live React component rendered inside Markdown.
</Alert>
```
